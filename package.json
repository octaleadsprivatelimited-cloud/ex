{
  "name": "consulting-website",
  "version": "1.0.0",
  "description": "Professional consulting website with Node.js and React",
  "main": "server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon backend/server.js",
    "client": "cd frontend && npm start",
    "build": "cd frontend && CI=false npm install && npm run build",
    "install-all": "npm install && cd frontend && npm install",
    "vercel-build": "cd frontend && npm install && CI=false npm run build"
  },
  "keywords": ["consulting", "business", "corporate"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "body-parser": "^1.20.2",
    "nodemailer": "^6.9.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "concurrently": "^8.2.2"
  }
}

